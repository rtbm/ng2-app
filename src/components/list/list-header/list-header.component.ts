import { Component } from '@angular/core';

@Component({
  selector: 'x-list-header',
  template: require('./list-header.component.html'),
  styles: [require('./list-header.component.scss')]
})

export class XListHeaderComponent {
}
