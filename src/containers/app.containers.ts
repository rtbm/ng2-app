import { ACCOUNT_CONTAINERS } from './account';
import { HEADER_CONTAINERS } from './header';
import { HOME_CONTAINERS } from './home';
import { USER_CONTAINERS } from './user';

export const APP_CONTAINERS = [
  ...ACCOUNT_CONTAINERS,
  ...HEADER_CONTAINERS,
  ...HOME_CONTAINERS,
  ...USER_CONTAINERS,
];
